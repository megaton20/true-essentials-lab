<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Attendance Tracker</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
</head>
<body>

  <div class="container my-5">

    <a href="/admin">back</a>
    <h2 class="mb-4">Active classes</h2>

    <table class="table table-bordered table-hover">
      <thead class="table-primary">
        <tr>
          <th>#</th>
          <th>Code</th>
          <th>Location</th>
          <th>Discount</th>
          <th>max users</th>
          <th>Current users</th>
          <th>Status</th>
          <th>Action</th>
        </tr>
      </thead>
      <tbody>
        <%classes.forEach(function(data,index) {%>
        <tr>
          <td><%= index +1 %></td>
          <td><%= data.code %></td>
          <td><%= data.location_name %></td>
          <td><%= data.discount_percentage %> %</td>
          <td><%= data.max_uses %></td>
          <td><%= data.current_users %></td>
          <td><%= data.is_active %></td>
       
          <td>
            <a href="/admin/class/<%= data.id%>">view</a>
            <button class="btn btn-success btn-sm" onclick="markPresent(this)">
              <i class="fas fa-check-circle"></i> Present
            </button>
          </td>
        </tr>
      <%}) %>
     
      </tbody>
    </table>
  </div>

  <script>
    function markPresent(button) {
      const row = button.closest('tr');
      const statusCell = row.querySelector('td:nth-child(4)');
      statusCell.innerHTML = '<span class="badge bg-success">Present</span>';
      button.disabled = true;
      button.innerHTML = '<i class="fas fa-check-double"></i> Marked';
    }
  </script>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
