<!DOCTYPE html>
<html>
<head>
  <title>Users - <%= seasonName %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container py-5">
  <h1>👥 Users for <%= seasonName %></h1>
  <table class="table mt-4">
    <thead><tr><th>Name</th><th>Email</th><th>Date Registered</th></tr></thead>
    <tbody>
      <% users.forEach(user => { %>
        <tr>
          <td><%= user.name %></td>
          <td><%= user.email %></td>
          <td><%= new Date(user.created_at).toLocaleString() %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
  <a href="/admin/seasons" class="btn btn-secondary">Back</a>
</body>
</html>
